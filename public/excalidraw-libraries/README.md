Drop `.excalidrawlib` files into this folder to preload them in SlideBoard.

Notes:
- Files are auto-discovered at runtime via `/api/excalidraw-libraries`.
- No code changes are needed when adding new library files.
- Libraries are merged into Excalidraw (not replacing existing user libraries).
